Не выполняя код, ответить, что выведет браузер и почему:
 
if (!("a" in window)) {
    var a = 1;
}
alert(a);
проверяем, содержится ли свойство "а" в объекте window. a определяется внутри if , и в момент выполнения проверки переменная a уже существует в window, но еще не определена.
Т.к. a существует мы не заходим в блок if, и a - undefined

var b = function a(x) {
    x && a(--x);
};
alert(a);
ошибка: а is not defined.

function a(x) {
    return x * 2;
}
var a;
alert(a);

объявление function a определяет значение переменной а. var a не переопределяет значение а. в результате a содержит в себе объявленную функцию.

function b(x, y, a) {
    arguments[2] = 10;
    alert(a);
}
b(1, 2, 3);
в функцию b передаем значения 1,2,3. через массив arguments меняем третье значение a (индекс=2) на 10. В результате выводим 10.

Правда я код выполнял и предсказать удалось не все )